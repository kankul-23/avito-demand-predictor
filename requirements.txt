# Avito Demand Predictor — зависимости проекта.
# Версии зафиксированы через `pip freeze` в рабочем .venv (Python 3.10)
# для воспроизводимости. Сгруппировано по назначению — не по алфавиту.

# --- Данные и признаки ---------------------------------------------------
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3
pyarrow==25.0.0          # чтение/запись .parquet (data/processed/*)

# --- Модели ----------------------------------------------------------------
scikit-learn==1.7.2      # LogisticRegression, TF-IDF, OneHot, StandardScaler, метрики
catboost==1.2.10         # финальная модель
lightgbm==4.7.0           # модель для сравнения (шаг 5 плана)
optuna==4.9.0             # тюнинг гиперпараметров (tune_catboost.py, tune_lightgbm.py)

# --- Трекинг экспериментов (шаг 6) -----------------------------------------
mlflow==3.15.1

# --- API (шаг 7) -------------------------------------------------------------
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4

# --- Тесты (шаг 8) -----------------------------------------------------------
pytest==9.1.1
httpx==0.28.1             # нужен FastAPI TestClient

# --- Визуализация (используется в notebooks/*.ipynb) ------------------------
matplotlib==3.10.9
plotly==6.9.0
graphviz==0.21
